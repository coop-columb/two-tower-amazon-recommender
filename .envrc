#!/bin/bash
# Direnv integration for automatic environment activation
# Install direnv: brew install direnv

# Check if in correct project directory
if [[ -f "activate_dev.sh" ]]; then
    echo "🎯 Two-Tower Recommender project detected"
    echo "   Auto-activating development environment..."
    source activate_dev.sh
fi
